import PropTypes from 'prop-types'
import './ComicForm.css'

const ComicForm = ({ comic, loading, onPromptChange, onStyleChange }) => {
  return <div className='prompt-input-container'></div>
}

export default ComicForm

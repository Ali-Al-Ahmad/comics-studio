import { Icon } from '@iconify-icon/react'
import PropTypes from 'prop-types'
import './ComicHeader.css'

const ComicHeader = ({ user, onBack }) => {
  return <div className='view-comic-header'></div>
}

export default ComicHeader
